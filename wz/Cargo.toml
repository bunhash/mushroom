[package]
name = "wz"
version = "0.1.0"
edition = "2021"

[lib]
name = "wz"
path = "src/lib.rs"
crate-type = ["lib"]

[dependencies]
tree = { version = "0.1.0", path = "../tree" }
crypto = { version = "0.1.0", path = "../crypto" }
